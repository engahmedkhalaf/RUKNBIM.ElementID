<Window x:Class="RUKNBIM.SmartSelect.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="License Manager — RUKNBIM" 
        Height="500" Width="440"
        Background="#0B0F19"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Topmost="True"
        WindowStyle="SingleBorderWindow"
        FontFamily="Segoe UI">
    
    <Window.Resources>
        <!-- Rounded Button Style -->
        <Style x:Key="RoundedButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#1E293B"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#334155"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Opacity" Value="0.9"/>
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Opacity" Value="0.5"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Rounded TextBox Style -->
        <Style x:Key="RoundedTextBoxStyle" TargetType="TextBox">
            <Setter Property="Background" Value="#111827"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#1F2937"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="CaretBrush" Value="White"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="border" Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="6">
                            <ScrollViewer x:Name="PART_ContentHost" Focusable="false" HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Hidden"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="true">
                                <Setter TargetName="border" Property="BorderBrush" Value="#3B82F6"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="20,15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/> <!-- 0. Header -->
            <RowDefinition Height="Auto"/> <!-- 1. Separator -->
            <RowDefinition Height="Auto"/> <!-- 2. Status Card -->
            <RowDefinition Height="Auto"/> <!-- 3. Email Input -->
            <RowDefinition Height="Auto"/> <!-- 4. Code Input -->
            <RowDefinition Height="Auto"/> <!-- 5. Main Action Button -->
            <RowDefinition Height="*"/>    <!-- 6. Footer -->
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <StackPanel Grid.Column="0">
                <TextBlock Text="RUKNBIM Smart Select" Foreground="White" FontSize="17" FontWeight="Bold"/>
                <TextBlock Text="License &amp; Activation Center" Foreground="#6B7280" FontSize="11" FontWeight="SemiBold" Margin="0,2,0,0"/>
            </StackPanel>

            <Border Grid.Column="1" Background="#070A13" BorderBrush="#1F2937" BorderThickness="1" CornerRadius="4" Padding="8,4" VerticalAlignment="Center" Cursor="Hand" MouseLeftButtonDown="RuknBimBadge_Click">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="RUKN" Foreground="White" FontWeight="Bold" FontSize="11"/>
                    <TextBlock Text="BIM" Foreground="#3A86FF" FontWeight="Bold" FontSize="11"/>
                </StackPanel>
            </Border>
        </Grid>

        <!-- Separator Line -->
        <Rectangle Grid.Row="1" Fill="#1F2937" Height="1" Margin="0,12"/>

        <!-- License Status Card -->
        <Border Grid.Row="2" Background="#131926" BorderBrush="#1F2937" BorderThickness="1" CornerRadius="8" Padding="15" Margin="0,0,0,15">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Bullet and Title -->
                <StackPanel Grid.Row="0" Orientation="Horizontal">
                    <Ellipse x:Name="StatusDot" Fill="#EF4444" Width="8" Height="8" VerticalAlignment="Center" Margin="0,0,8,0"/>
                    <TextBlock Text="License Status" Foreground="#9CA3AF" FontSize="11" FontWeight="SemiBold"/>
                </StackPanel>

                <!-- Status Value -->
                <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,10,0,0">
                    <TextBlock Text="Status:" Foreground="#6B7280" Width="60" FontSize="12"/>
                    <TextBlock x:Name="StatusTextVal" Text="Not Activated" Foreground="#EF4444" FontWeight="Bold" FontSize="12"/>
                </StackPanel>

                <!-- Details Value -->
                <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,6,0,0">
                    <TextBlock Text="Details:" Foreground="#6B7280" Width="60" FontSize="12"/>
                    <TextBlock x:Name="DetailsTextVal" Text="No active license found." Foreground="#9CA3AF" TextWrapping="Wrap" MaxWidth="320" FontSize="12"/>
                </StackPanel>

                <!-- Test Connection Button -->
                <Button Grid.Row="3" x:Name="BtnTestConn" Content="Test Connection" Style="{StaticResource RoundedButtonStyle}" 
                        Background="#1E293B" Foreground="#9CA3AF" BorderBrush="#334155" Width="110" Height="24" 
                        HorizontalAlignment="Left" Margin="0,12,0,0" FontSize="11" Click="BtnTestConn_Click"/>
            </Grid>
        </Border>

        <!-- Registered Email Input -->
        <StackPanel Grid.Row="3" Margin="0,0,0,12">
            <TextBlock Text="Registered Email" Foreground="#9CA3AF" FontSize="11" Margin="0,0,0,5"/>
            <TextBox x:Name="EmailInput" Style="{StaticResource RoundedTextBoxStyle}" Height="30" VerticalContentAlignment="Center"/>
        </StackPanel>

        <!-- Activation Code Input -->
        <StackPanel Grid.Row="4" Margin="0,0,0,15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="Activation Code" Foreground="#9CA3AF" FontSize="11" VerticalAlignment="Center"/>
                <TextBlock Grid.Column="1" Text="Request Activation Code" Foreground="#3A86FF" FontSize="11" VerticalAlignment="Center" 
                           Cursor="Hand" TextDecorations="Underline" MouseLeftButtonDown="RequestCode_Click"/>
            </Grid>
            <TextBox x:Name="CodeInput" Style="{StaticResource RoundedTextBoxStyle}" Height="30" Margin="0,5,0,0" VerticalContentAlignment="Center"/>
        </StackPanel>

        <!-- Main Action Button -->
        <Button Grid.Row="5" x:Name="BtnActivate" Content="Activate License" Style="{StaticResource RoundedButtonStyle}" 
                Background="#2563EB" Foreground="White" BorderThickness="0" Height="36" FontSize="13" FontWeight="Bold" 
                Click="BtnActivate_Click"/>

        <!-- Footer -->
        <Grid Grid.Row="6" VerticalAlignment="Bottom" Margin="0,10,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                <TextBlock Text="Support: " Foreground="#6B7280" FontSize="11"/>
                <TextBlock Text="support@ruknbim.com" Foreground="#3A86FF" FontSize="11" Cursor="Hand" TextDecorations="Underline" MouseLeftButtonDown="SupportLink_Click"/>
            </StackPanel>

            <Button Grid.Column="1" x:Name="BtnClose" Content="Close" Style="{StaticResource RoundedButtonStyle}" 
                    Background="#1E293B" Foreground="White" Width="70" Height="26" Click="BtnClose_Click"/>
        </Grid>
    </Grid>
</Window>
